# ============================================================
# SFTP MODE: LOCAL EMBEDDED (direct connection)
# ============================================================
# Use this when CUCM can reach your machine directly.
# The app runs its own SFTP server on port 2222 and CUCM
# uploads files directly to it.
#
# With network_mode: host, the container shares your PC's
# network - it can reach CUCM and CUCM can reach back to
# the embedded SFTP server. The SFTP host IP is auto-detected
# from your machine's network interfaces.
#
# To activate: cp .env.local .env && docker compose up -d
# ============================================================

SFTP_RELAY_MODE=false
SFTP_SERVER_ENABLED=true

# Embedded SFTP server settings
SFTP_SERVER_HOST=0.0.0.0
SFTP_SERVER_PORT=2222

# SFTP credentials CUCM will use to connect to this server
# SFTP_HOST is auto-detected from host network - leave empty
SFTP_HOST=
SFTP_PORT=2222
SFTP_USERNAME=cucm-collector
SFTP_PASSWORD=cucm-collector
SFTP_REMOTE_BASE_DIR=

# ============================================================
# App Settings (usually no need to change)
# ============================================================
LOG_LEVEL=INFO
CORS_ALLOWED_ORIGINS=.*
