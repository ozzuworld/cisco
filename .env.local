# ============================================================
# SFTP MODE: LOCAL / DIRECT (self-contained, embedded SFTP)
# ============================================================
# The app runs its own SFTP server on port 2222 inside the
# container. CUCM pushes files here via "file get activelog".
#
# You MUST set SFTP_HOST to your machine's IP on the CUCM
# network so CUCM knows where to connect.
# Find it with: ipconfig (Windows) or ip addr (Linux/Mac)
#
# Docker Desktop (Windows/Mac): ensure Windows Firewall
# allows inbound TCP port 2222:
#   New-NetFirewallRule -DisplayName 'CUCM SFTP' -Direction Inbound -Protocol TCP -LocalPort 2222 -Action Allow
#
# To activate: cp .env.local .env && docker compose up -d
# ============================================================

SFTP_RELAY_MODE=false
SFTP_SERVER_ENABLED=true

# Embedded SFTP server settings
SFTP_SERVER_HOST=0.0.0.0
SFTP_SERVER_PORT=2222

# SFTP credentials CUCM will use to push files to the embedded server
# SET SFTP_HOST to your PC's IP address (e.g. 192.168.1.100)
SFTP_HOST=
SFTP_PORT=2222
SFTP_USERNAME=cucm-collector
SFTP_PASSWORD=cucm-collector
SFTP_REMOTE_BASE_DIR=

# ============================================================
# App Settings (usually no need to change)
# ============================================================
LOG_LEVEL=INFO
CORS_ALLOWED_ORIGINS=.*
