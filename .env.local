# ============================================================
# SFTP MODE: LOCAL / DIRECT (CUCM pushes to your machine)
# ============================================================
# CUCM retrieves files using "file get activelog", which pushes
# files outbound to an external SFTP server. CUCM does NOT
# support inbound SFTP (pull) or SCP.
#
# === DOCKER DESKTOP (Windows/Mac) ===
# Docker Desktop's port forwarding breaks SSH key exchange,
# so CUCM cannot connect to the container's SFTP server.
# Instead, point SFTP settings at your HOST machine's SSH server:
#
#   SFTP_HOST=<your PC's IP on the CUCM network>
#   SFTP_PORT=22
#   SFTP_USERNAME=<your Windows/Mac login username>
#   SFTP_PASSWORD=<your Windows/Mac login password>
#   SFTP_REMOTE_BASE_DIR=<absolute path to ./storage/received>
#   SFTP_SERVER_ENABLED=false
#
# Example (Windows with OpenSSH server enabled):
#   SFTP_HOST=192.168.1.100
#   SFTP_PORT=22
#   SFTP_USERNAME=osman
#   SFTP_PASSWORD=mypassword
#   SFTP_REMOTE_BASE_DIR=C:/Users/osman/cisco/storage/received
#
# === NATIVE LINUX DOCKER ===
# The container's embedded SFTP server on port 2222 works fine.
# Just set SFTP_HOST to your machine's IP. Leave other defaults.
#
# To activate: cp .env.local .env && docker compose up -d
# ============================================================

SFTP_RELAY_MODE=false
SFTP_SERVER_ENABLED=true

# Embedded SFTP server settings (native Linux Docker only)
SFTP_SERVER_HOST=0.0.0.0
SFTP_SERVER_PORT=2222

# SFTP credentials CUCM will use to push files
# SET SFTP_HOST to your PC's IP address (e.g. 192.168.1.100)
SFTP_HOST=
SFTP_PORT=2222
SFTP_USERNAME=cucm-collector
SFTP_PASSWORD=cucm-collector
SFTP_REMOTE_BASE_DIR=

# ============================================================
# App Settings (usually no need to change)
# ============================================================
LOG_LEVEL=INFO
CORS_ALLOWED_ORIGINS=.*
