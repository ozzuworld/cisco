# ============================================================
# SFTP MODE: RELAY (for VPN users)
# ============================================================
# Use this when CUCM cannot reach your machine directly.
# CUCM uploads to an external relay SFTP server, then the app
# downloads files from that server.
#
# To activate: cp .env.relay .env && docker compose up -d
# ============================================================

SFTP_RELAY_MODE=true
SFTP_SERVER_ENABLED=false

# Relay SFTP server that CUCM will upload to
SFTP_HOST=104.156.42.4
SFTP_PORT=22
SFTP_USERNAME=wx001-sftp
SFTP_PASSWORD=your-password-here
SFTP_REMOTE_BASE_DIR=cucm-captures

# ============================================================
# App Settings (usually no need to change)
# ============================================================
LOG_LEVEL=INFO
CORS_ALLOWED_ORIGINS=.*
