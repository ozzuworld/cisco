# Production Environment Variables
# Used when building for Docker/Production

# Backend API URL - Empty means same origin (no CORS issues)
# Frontend and backend are served from the same host:port
VITE_API_BASE_URL=

# API Version
VITE_API_VERSION=v1
